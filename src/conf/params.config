params{

    datadir = "/users/cn/lsantus/data/FED/"
    outdir = "/users/cn/lsantus/data/FED/out"

    assemblies = "${params.datadir}/assemblies/"
    annotations = "${params.datadir}/annotations/"
    gene_lists = "${params.datadir}/gene_lists/"
    metadata = "${params.datadir}/metadata/"

}